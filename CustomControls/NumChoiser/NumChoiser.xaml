<UserControl x:Class="SPWPF.CustomControls.NumChoiser.NumChoiser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SPWPF.CustomControls.NumChoiser"
             mc:Ignorable="d"
             
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:windowcontrolpanel="clr-namespace:StepChat.StepChatUI.CustomUIElement.WindowControlPanel"
        xmlns:viewmodel="clr-namespace:SPWPF.MVVM.ViewModel.MainWindowViewModel"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"

        
     FontFamily="{DynamicResource MaterialDesignFont}" Height="100" Width="100"  
             >
    <Border CornerRadius="5" Background="{DynamicResource PrimaryHueMidBrush}">
        <Button Command="{Binding SelectNumberClick}" Name="NumText"  Margin="15" FontSize="45" FontWeight="Bold" FontFamily="Bernard MT Condensed" Background="{DynamicResource PrimaryHueDarkBrush}" CommandParameter="{Binding ElementName=NumText}" Height="Auto" ></Button>
    </Border>
</UserControl>
